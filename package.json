{
  "name": "tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --spec cypress/integration/tests/*.js",
    "tests-chrome": "npx cypress run --browser chrome"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^8.3.1"
  },
  "devDependencies": {
    "dotenv": "^10.0.0"
  }
}
